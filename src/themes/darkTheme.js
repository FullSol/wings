import { createTheme } from "@mui/material/styles";

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    // customize your colors here
  },
  // customize other theme aspects like typography, breakpoints, etc.
});

export default darkTheme;
